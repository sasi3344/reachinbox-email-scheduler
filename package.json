{
  "name": "reachinbox-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm run start"
  }
}
